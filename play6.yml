---
- name: This is able to creater a softlink
  hosts: localhost
  become: true
  tasks:
   - name: creater an empty file
     ansible.builtin.file:
       src: /home/jenkins/lik
       dest: /home/jenkins/lik1
       state: link #Soft link is create
       owner: ansible
       group: jenkins
       mode: "0570"
...
